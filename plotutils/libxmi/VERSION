The version of libxmi in this directory is 1.3, age=1.  (Age=1 means that
its interfaces are backward compatible with libxmi 1.x and 0.x.)

The library version number is not necessarily the same as the version
number of the libxmi package.  However, we are now trying to keep them the
same.
